# Order Book Reconstruction Verification

> [!IMPORTANT]
> **Verification Status**: PASSED (with key insights)

We have successfully reconstructed the order book snapshots for `20251231` (Near/Next Term) and verified them against official PROD files.

## 1. Strategy Comparison

We tested two reconstruction strategies:

1. **My_Last (Naive)**: Selects the absolute latest quote before the snapshot time.
2. **My_Min (Robust)**: Selects the quote with the **Minimum Spread** within a 15-second window (Official VIX logic).

## 2. Key Findings: Official Data Structure

Detailed audit of `NextPROD_20251231.tsv` revealed it records **two sets** of data:

1. **Columns 3-8**: Correspond to the **Min Spread** logic (e.g., Strike 30800 Bid=164).
2. **Columns 42-47 (Snapshot)**: Correspond to the **Last Quote** logic (e.g., Strike 30800 Bid=49).

The user's goal is to reproduce the **final snapshot columns**.

## 3. Verification Results (Target: Snapshot Columns)

We verified the **My_Last (Naive)** strategy against the Official `snapshot_call_bid` / `snapshot_put_bid` columns across all 4 scenarios:

| Scenario | Result | Discrepancies |
| :--- | :--- | :--- |
| **Near Call** | **PASS** | 0 |
| **Near Put** | **PASS** | 0 |
| **Next Call** | **PASS** | 0 |
| **Next Put** | **PASS** | 0 |

## 4. Milestone: Phase 2 VIX Index Calculation

- **Status**: Completed (2026-02-24)
- **Changes**: Implemented standard interpolation and degenerate formulas for near-expiry cases. Supported VIX rounding to 2 decimal places and pre-open disclosure rules.
- **Verification**: Cross-checked against official VIX files; confirmed identical values for both normal and corner cases.

## 5. Milestone: Phase 3 Visualization & Analysis Tools

- **Status**: Completed (2026-02-25)
- **Highlights**:
  - **Alert System**: Built automated detection and UI reporting for significant VIX jumps.
  - **Explorer Mode**: Implemented full-day tick flow recovery for deep-dive analysis.
  - **MSCI Dashboard**: Established interactive visualization for index movement comparisons.

## 6. Milestone: UI/UX Refinement & Persistence Implementation

- **Status**: Completed (2026-02-26)
- **Highlights**:
  - **Notion-Style UI**: Successfully redesigned the entire Viewer interface with a modern, clean, and professional aesthetic modeled after Notion. Optimized font scaling (base 14px/15px) and spacing for better readability.
  - **Persistence Mechanism**: Implemented `sessionStorage` for date selection and tab focus. The application now remembers the last session state (including the active mode: Dashboard, Diff, Explore, Sigma) after page refreshes.
  - **Enhanced Explorer Visibility**: Added "Source Mapping" to the Snapshot Divider in the Explore mode. Users can now directly see if a snapshot quotation came from the current interval calculation ($Q̂$) or a preceding valid quote (Replacement/之前有效報價).
  - **Backend Stabilization**: Refined API response structures and fixed CSS encoding/redundancy issues to ensure a smooth, stable user experience.
